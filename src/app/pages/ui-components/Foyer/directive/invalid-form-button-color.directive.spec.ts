import { InvalidFormButtonColorDirective } from './invalid-form-button-color.directive';

describe('InvalidFormButtonColorDirective', () => {
  it('should create an instance', () => {
    const directive = new InvalidFormButtonColorDirective();
    expect(directive).toBeTruthy();
  });
});
